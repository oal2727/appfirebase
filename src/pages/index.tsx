import React from "react"
import Typography from '@material-ui/core/Typography';

const index = ()=>{
    return(
        <Typography variant="h5" style={{textAlign:"center"}} gutterBottom>
          App Authentication with Firebase
      </Typography>
    )
}
export default index;